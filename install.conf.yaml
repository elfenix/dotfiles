- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.config/kitty/kitty.conf: kitty.conf
    ~/.scripts: scripts

- shell:
    - [git submodule update --init --recursive, Installing submodules]

